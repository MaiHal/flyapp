CREATE TABLE IF NOT EXISTS "aircrafts"("id" integer PRIMARY KEY AUTOINCREMENT NOT NULL, "file_name" string NOT NULL,"airline" string DEFAULT NULL,"aircraft_type" string DEFAULT NULL,"airport" string DEFAULT NULL,"taken_at" datetime DEFAULT NULL, "camera_model" string DEFAULT NULL, "user_id" integer NOT NULL, "posted_at" datetime DEFAULT(CURRENT_TIMESTAMP) NOT NULL);