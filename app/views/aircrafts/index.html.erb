<!DOCTYPE html>
<html>
  <head>
    <title>Flyapp</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="//maxcdn.bootstrapcdn.com/font-awesome/4.5.0/css/font-awesome.min.css">
    <%= stylesheet_link_tag    'aircrafts', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'aircrafts', 'data-turbolinks-track': 'reload' %>
  </head>
  <body>
    <header>
      <div class="container">
        <div class="header-left">
          <img class="logo" src="/logos/logo.png">
        </div>
        <div class="header-center">
            <a href="/">TOP</a>
            <a href="/reports">REPORT</a>
            <a href="#">NEWS</a>
            <a href="#">USERS</a>
        </div>
        <div class="header-right">
            <a href="#">新規登録</a>
            <a href="#" class="login">ログイン</a>
        </div>
      </div>
    </header>

    <div class="photos-wrapper">
      <div class="container">
        <div class="photos">
          <h1>Photos</h1>
            <% @aircrafts.each do |aircraft| %>
              <div class="photo">
                <img class="photo" src="<%= "/images/aircraft/#{aircraft.file_name}" %>">
                <div class="photo-data">
                  <p class="photo-contents">空港 : <%= @airports.find_by(id: aircraft.airport_id)[:cord] %></p>
                  <p class="photo-contents">航空会社 : <%= @airlines.find_by(id: aircraft.airline_id)[:cord] %></p>
                  <p class="photo-contents">機材 : <%= aircraft.aircraft_type %></p>
                  <p class="photo-contents">投稿者 : <%= aircraft.user_id %></p>
                </div>
              </div>
              <% if aircraft.id%4 == 0 %>
                <br>
              <% end %>
            <% end %>
          <div class="clear"></div>
        </div>
        <%= will_paginate @aircrafts, renderer: BootstrapPagination::Rails %>

        </div>
      </div>
    </div>

    <footer>
      <div class="container">
        <img src="/logos/footer_logo.png">
        <p>let's join aviation community</p>
      </div>
    </footer>
  </body>
</html>